
class Platform {

    constructor(width, height, type, x, y) {
        this.width = width;
        this.height = height;
        this.type = type;
        this.x = x;
        this.y = y;
    }
    
    Create() {
       for(i = 0; i < this.width; i++) {

        if i == 0 {
            
        }


       }

    }
}

const platform = new Platform(100, 100, "grass", 0, 0);

platform.Create()